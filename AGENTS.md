# エージェント共通ルール

このリポジトリで作業するAIエージェント(Codex / Claude Code / Cursor / Antigravity など)へ:
共通ルールの全文は `C:\Users\jmfs2\projects\AGENTS.md` にある。読める環境なら必ず読むこと。

## 要点(全文が読めない場合はこれに従う)

- `C:\Users\jmfs2\projects\` が全プロジェクトの唯一の正式な置き場所。**別の場所に新しくcloneしない**
- 作業開始時に `git pull`、**作業終了時に必ずコミット**する(ユーザーは複数のAIツールを併用しており、引き継ぎはGitコミット経由で行う)
- 大きな変更は `feat/〜` `fix/〜` ブランチを切る。`main` に直接大量の変更を積まない
- ユーザーへの説明・コミットメッセージは日本語で書く
- `.env` はコミットしない
